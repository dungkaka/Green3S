export const useFetchChapterAuthorAPI = (story_id, status = "", page) => {
    // const res = useAPIFetcher(API_TRUYEN.GET_LIST_CHAPTERS_OF_AUTH_STORY(story_id, status, page, 20));
    // return res;
};

export const useFetchChapterAPI = (isReady, story_id) => {
    // console.log("A", isReady, story_id);
    // const res = useAPIFetcher(isReady ? API_TRUYEN.GET_LIST_CHAPTERS_OF_STORY(story_id, "activate") : null, {
    //     dedupingInterval: 5000,
    // });
    // return res;
};
