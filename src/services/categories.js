
export const useFetchCategoriesAPI = () => {
    // return useAPIFetcher(API_TRUYEN.GET_CATEGORIES());
};

export const useFetchStoriesOfCategory = (id) => {
    // const res = useAPIFetcher(API_TRUYEN.GET_STORIES_OF_CATEGORY(id), {
    //     revalidateOnMount: false,
    // });
    // const { data, mutate, isValidating } = res;
    // useEffect(() => {
    //     if (!data && !isValidating) mutate();
    // }, [!!data]);
    // return res;
};
